<div class="cross-lockable-screen">
  <div class="cross-lockable-screen-box"></div>

  <script type="text/html">
    <%= form_for(current_user, url: "#{AccountsIntegration.accounts_url}/users/refresh_session?&return_to=#{dui.user_session_refreshed_url}", method: :post, html: { target: 'cross-lockable-iframe' }) do |f| %>

      <h2>Sessão travada!</h2>

      <p>Por questões de segurança a aua sessão está travada.</p>
      <p>Digite sua senha abaixo para destravá-la.</p>

      <fieldset>
        <%= f.hidden_field :email, current_user.email %>

        <div class="control-group password">
          <div class="controls">
            <div class="input-prepend">
              <span class="add-on"><i class="icon-asterisk"></i></span>

              <%= f.text_field :password, required: false %>

              <span class="help-block lock-screen-error hide">Senha inválida.</span>
            </div>
          </div>
        </div>
      </fieldset>

      <div class= "actions">
        <%= button_tag :submit, class: 'btn btn-success btn-large', data: { disable_with: 'Entrando...' } do %>
          <%= content_tag(:i, nil, class: 'icon-off icon-white') %>
          <%= 'Entrar' %>
        <% end -%>
      </div>
    <% end %>
  </script>

  <iframe id="cross-lockable-iframe"></iframe>
</div>